import {
  OpenAPIObject,
  OperationObject,
  PathItemObject,
  ReferenceObject,
  SchemaObject
} from '@loopback/openapi-v3-types';

export class Generator {
  generate(): void {
    const a = OpenAPIObject.abc;
  }
}
